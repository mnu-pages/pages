.TITLE "wsl"

.DIV "DESCRIPTION"
Windows Subsystem for Linux allows developers to run a GNU/Linux environment directly on Windows. It provides a seamless experience for running __command line tools__ and __applications__ without the overhead of a traditional virtual machine.

.DIV "TLDR"
**wsl**
Starts the default Linux distribution in the current terminal.
> 

**wsl --install**
Installs the default Ubuntu distribution and the necessary subsystem components.
> __--install__ install flag

**wsl --list --online**
Displays a list of all available Linux distributions that can be installed.
> __--list__ list flag | __--online__ remote source flag

**wsl --distribution name**
Runs a specific Linux distribution by its name.
> __--distribution__ distribution flag | __name__ the name of the distro

**wsl --update**
Checks for and installs updates to the WSL kernel.
> __--update__ update flag

**wsl --shutdown**
Terminates all running distributions and the WSL 2 lightweight utility VM.
> __--shutdown__ power off flag

.DIV "SUMMARY"
Always use __wsl --shutdown__ if you notice high memory usage from the Vmmem process. For the best performance and compatibility, ensure you are using __WSL 2__ instead of the legacy version 1.

.DIV "SEE ALSO"
**cli:powershell** **cli:ssh** **cli:bash**

documentation: __https://learn.microsoft.com/windows/wsl__
license: Custom License (__https://github.com/microsoft/WSL/blob/master/LICENSE__)

.DIV "AUTHOR"
written by mnu contributors

