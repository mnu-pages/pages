.TITLE "git"

.DIV "DESCRIPTION"
A distributed version control system designed to handle everything from small to very large projects with speed and efficiency. It tracks __changes__ in source code and manages __collaborative workflows__.

.DIV "TLDR"
**git init**
__Creates__ an empty Git __repository__ or reinitializes an existing one.
> 

**git add .**
__Adds__ all modified and new files in the current directory to the __staging area__.
> __.__ current directory reference

**git commit -m "message"**
__Records__ changes to the __repository__ with a descriptive message.
> __-m__ message flag | __"message"__ the commit description

**git branch name**
__Creates__ a new __branch__ with the specified name to isolate features.
> __name__ the new branch name

**git checkout branch**
__Switches__ the current working environment to a different __branch__.
> __branch__ the target branch name

**git log --oneline**
__Displays__ a compact __history__ of commits showing only the hash and message.
> __--oneline__ condensed format flag

.DIV "SUMMARY"
Regularly using __git status__ alongside __git log --oneline__ provides a clear overview of your project's state. When using __git add .__, ensure your __.gitignore__ file is properly configured to avoid staging unnecessary files.

.DIV "SEE ALSO"

documentation: __https://git-scm.com/doc__
license: GPL-2.0

.DIV "AUTHOR"
written by mnu contributors

